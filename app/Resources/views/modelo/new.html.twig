{% extends 'base.html.twig' %}

{% block contenido %}
<div id="page-wrapper">
        <div class="row">
    <h1>Nuevo Modelo</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" class="btn btn-success" value="Create" />
    {{ form_end(form) }}

    <a class="btn btn-warning" href="{{ path('modelo_index') }}">Back to the list</a>
</div>
</div>
{% endblock %}
