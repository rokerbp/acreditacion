{% extends 'base.html.twig' %}
{% form_theme edit_form 'form/itemForm.html.twig' %}

{% block titulo %}
    Editar Factor
{% endblock %}


{% block contenido %}
<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Editar Factor</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-md-8">

            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <p>El valor de la ponderacion no puede ser mayor a {{ 100 - pond }}</p>
                <input class="btn btn-success" type="submit" value="Guardar" />
                <a class="btn btn-warning" href="{{ path('factor_show', {'id': factor.id}) }}">Atras</a>
            {{ form_end(edit_form) }}
    </div>
</div>
{% endblock %}
